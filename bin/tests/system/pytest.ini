[pytest]
addopts = --tb=short -rA --ignore-glob="*-tmp-*"
log_format = %(asctime)s %(levelname)s:%(name)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_cli = 1
log_level = INFO
python_files = tests_*.py
